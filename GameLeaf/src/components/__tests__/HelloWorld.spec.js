import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import LogoTagline from '../LogoTagline.vue'

describe('LogoTagline', () => {
  it('renders properly', () => {
    // const wrapper = mount(LogoTagline, { props: { msg: 'Hello Vitest' } })
    // expect(wrapper.text()).toContain('Hello Vitest')
  })
})
